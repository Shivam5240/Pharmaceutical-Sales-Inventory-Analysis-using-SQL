-- Which region has the highest total units sold?

-- SELECT
--     s.region,
--     SUM(s.units_sold) AS total_units_sold
-- FROM drug_sales s
-- GROUP BY s.region
-- ORDER BY total_units_sold DESC;

-- INTERPRETATION !!!
-- East region shows the highest sales volume in the simulated dataset, suggesting that it would be the strongest-performing region for further investigation.